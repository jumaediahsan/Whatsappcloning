import React, { Component } from 'react';
import { Text } from 'react-native';
import { Button } from 'react-native-elements';

class Calling extends Component {
  render() {
    return (
        <Text>cals belum aktif</Text>
    )
  }
}

export default Calling;
